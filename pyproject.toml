[project]
name = "ai_playground"
version = "0.1.0"
description = "Terminal-based hobby application."
readme = "README.md"
authors = [
    { name = "Abdelaziz Khabthani" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ai-playground = "ai_playground.main:main"

[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"
